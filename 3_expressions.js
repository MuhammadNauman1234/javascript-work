// Expressions are combinations of variables, values, and operators that can be evaluated to produce a result.

let x = 10;
let y = 5;
let result = x + y * 2; // expression combinition of variables, values and operators

console.log(result);